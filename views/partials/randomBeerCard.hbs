<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beer Card</title>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-4">
        <img
      class="card-img-top"
      src= {{image_url}}
      alt=""/>
      </div>
      <div class="col-md-8">
      <h5 class="card-title">{{name}}</h5>
      <p class="card-text">{{description}}</p>
      <p class="card-text">{{tagline}}</p>
      <p class="card-text">Food pairing:{{food_pairing}}</p>
      <p class="card-text">Brewer tips:{{brewers_tips}}</p>
      </div>
    </div>
  </div>




</div>
</body>
</html>